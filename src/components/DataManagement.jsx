import "./DataManagement.css";

const DataManagement = () => {
  return (
    <div className="data-management">
      {/* Panelist export removed from header. Use Panelist tab for export. */}
    </div>
  );
};

export default DataManagement;
